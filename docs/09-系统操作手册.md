# 09 · 系统操作手册

> 这份手册面向**使用者**，不面向改代码的人。按「我要做什么」组织，每一步都能照着点完。
> 想看设计取舍与实现细节，去 02-架构设计；想知道为什么某处这么绕，去 07-踩坑记录。

---

## 0. 这份手册怎么用

- **第一次部署**：读第 1 章，照抄即可。
- **不知道某个功能在哪**：读第 2 章（选项卡导览），五分钟能建立全图。
- **正在做具体一件事**：直接跳第 3 章的对应小节。
- **遇到报错或异常**：跳第 5 章，按症状查。
- **查字段名 / 默认值 / 环境变量 / 接口路径**：查附录 A（由代码自动生成，永远与实现同步）。

一条贯穿全篇的原则：**能在面板里改的，就不要去改代码或环境变量**。
面板配置存在存储里，保存即生效；环境变量只是首次部署的种子（见第 4 章）。

---

## 1. 第一次上线

### 1.1 需要准备什么

| 东西 | 说明 |
|---|---|
| Cloudflare 账号 | 免费套餐够用 |
| 一个接进 Cloudflare 的域名 | 用于自定义域名与 DNS 优选（可选，但强烈建议） |
| D1 数据库 或 KV 命名空间 | 二选一，默认 D1。用来存站点配置、各功能的面板配置 |
| GitHub 仓库 | 部署走 Actions，push 即上线 |

数据库与 KV 的绑定写在 `wrangler.toml` 里，**绑定名不要改**（`SITES` 是代码里写死要找的名字）。

### 1.2 上线步骤

1. **不用手动建存储资源**：部署脚本会复用 / 自动创建 D1 数据库（默认名 `any-proxy-db`）、KV 命名空间（绑定名 `SITES`）与 R2 桶（`anyproxy-media`）。想指定已有资源才需要在仓库 Variables 里填 `D1_DATABASE_ID` / `KV_NAMESPACE_ID` / `R2_BUCKET_NAME`。
2. 在 GitHub 仓库的 **Settings → Secrets and variables → Actions** 里配置 Secrets（至少 `CLOUDFLARE_API_TOKEN`、`CLOUDFLARE_ACCOUNT_ID`、`PASSWORD`）。
3. `git push origin master`。
4. Actions 会自动：渲染 `wrangler.toml` 的占位符（缺的资源自动创建）→ 按 `STORAGE_BACKEND` 裁剪绑定 → 应用数据库迁移 → 部署 Worker → 写入 Secrets。
5. 等 Actions 跑完（Verify + Deploy 两步都绿），访问你的域名，看到登录页就成功了。

> 表结构由 `migrations/0001_init.sql` 定义，部署时自动应用，不需要手动执行 SQL。

### 1.3 三个地址，记住就行

| 地址 | 作用 |
|---|---|
| `https://<你的域名>/__login` | 登录入口。**开了首页伪装之后，这个地址会先要求走隐蔽路径** |
| `https://<你的域名>/__admin` | 管理面板。所有配置都在这里 |
| `https://<你的域名>/p/<站点后缀>/...` | 反代访问入口，一个站点一个前缀 |

其余保留路径（不要拿来做站点后缀）：`/p/`、`/sub`、`/tsub/`、`/edt`、`/admin`、`/__`、以及临时链接的默认前缀 `/s`。

### 1.4 首次登录后，建议先做三件事

1. **改口令并配好隐蔽入口**：`伪装与安全` 选项卡 → 首页伪装 → 设一个隐蔽路径（例如 `/mypanel`）和进门口令。
   不开伪装也没关系，但一旦对外，这一步决定了别人能不能猜到这是个代理面板。
2. **加一个站点**：`站点` 选项卡 → 填名称和网址 → 保存。立刻就能通过 `/p/<后缀>/` 访问。
3. **配优选与通知**（可选但推荐）：
   - `优选 IP` 选项卡 → 填订阅链接 → 点「浏览器自动优选」（测完自动写入优选池并记录延迟）
     → 再点「立即更新优选 IP」写入 A 记录；
   - `告警通知` → 填 Webhook → 点「发一条测试告警」确认能收到。

---

## 2. 界面导览：每个选项卡管什么

面板顶部一排选项卡，**一块功能只有一个归属地**。这条规矩是有代价换来的：
优选池曾经同时长在「优选 IP」和「配置」两页里，改完一边忘了另一边就会出现
「面板显示 A、实际生效 B」，排查起来极费时间。

| 选项卡 | 管什么 | 不该在这里找什么 |
|---|---|---|
| **站点** | 反代站点的增、删、改、复制链接 | 其他功能的开关 |
| **数据驾驶舱** | 访问统计的可视化（KPI / 每日趋势 / 通道排行）与统计开关、保留策略 | 站点与代理配置 |
| **CF 用量** | Cloudflare 侧用量驾驶舱（需先填 CF API 凭据，在「配置中心 → 运行参数」里） | 站点与代理配置 |
| **代理节点** | 订阅链接、节点国家标注、通往代理面板与临时订阅页的入口 | 优选 IP 的配置 |
| **优选 IP** | 自动优选频率、立即优选、优选 IP 池、**候选域名池与健康检查**、浏览器自动优选 | 站点设置 |
| **伪装与安全** | 首页伪装的模板、文案、隐蔽路径、进门口令、严格模式 | — |
| **外观主题** | 10 套内置主题、自定义主题、自动轮换 | 页面内容 |
| **临时链接** | 临时链接的开关与默认值、生成、停用、删除 | — |
| **配置中心** | **运行参数**（优选频率 / 池上限 / 探测门限 / 订阅策略 / 外部数据源地址 / CF API 凭据 / 面板运维入口 / 哈希盐值…）+ 站点模式注册表与代码级字典的只读真源位置 | 站点/主题/伪装 的具体表单（都在各自页里） |
| **配置** | 只放**没有独立选项卡**的设置：限流防滥用、告警通知；外加诊断工具，以及通往上面各页的入口 | 站点/主题/伪装/优选/统计 的配置表单（都在各自页里） |

「配置」这一页现在很短，这是刻意的：它只做两件事 —— 摆开没人管的设置，以及当索引。
**「配置中心 → 运行参数」**是这次配置统一化后的新归属地：凡是「以前只能靠环境变量改、
改完要重新部署」的项，现在都在这张卡片里，保存后立即生效。

---

## 3. 日常操作

### 3.1 加一个反代站点

**站点** 选项卡 → 名称 + 网址 → 保存。

- **访问后缀**留空会自动生成（由名称推出来）；自己填要避开 1.3 里那串保留段。
- 加完就能用 `https://<你的域名>/p/<后缀>/` 打开目标站，站内链接、CSS、JS 都会被自动改写。
- 改后缀会让旧链接失效，面板上会有提示。
- 删除站点后 `/p/<后缀>/` 立即失效，没有回收站。

### 3.2 节点与订阅

**代理节点** 选项卡：

- **订阅链接**：浏览器优选从这里拉候选 IP。留空则回退到本机 `/sub`。
  第三方订阅常混入非边缘网络的节点，系统会按 Cloudflare 官方 IP 段先过滤再测速 —— 不过滤的话，
  那些不可达地址会占满优选池，还会把「立即更新优选 IP」拖到超时。
- **节点国家标注**：给订阅里的节点备注补上归属国家，主订阅与临时订阅都生效。
  国家查询结果长期缓存，同一个 IP 只查一次；数据源挂了会自动跳过标注，**绝不影响订阅本身**。
- **代理面板** 与 **临时订阅管理**：这两个是独立页面，通过选项卡里的链接跳过去。
  临时订阅给的是独立 UUID，到期自动失效，适合临时分享。

### 3.3 优选 IP 与 DNS

**优选 IP** 选项卡，按顺序理解：

1. **自动更新频率**：定时任务多久跑一次优选（5 ~ 1440 分钟，默认 720 = 12 小时）。
2. **立即更新优选 IP**：不等周期，马上测通并改写 A 记录。
3. **订阅链接**：候选 IP 的来源。
4. **优选 IP 池**：真正的候选列表，每行一个 IPv4。保存只存列表，**不**动 DNS。
5. **浏览器自动优选**：从订阅拉候选，在**你自己的浏览器**里并发测速（约 5~15 秒），最快的自动写进池子，
   延迟数值一并存下来 —— 自动优选按它排序，**服务端只负责剔掉不通的**。
   为什么必须在浏览器里测：出口在你那边，测出来的延迟才是你真正会遇到的（服务端只能从 Cloudflare
   自己的网络往外探，那个快慢跟你的网络基本无关 —— 实测两个视角的排序近似零相关）。
   测速时请**不要开着代理访问本页**，否则测到的是「出口节点 → CF」而不是「你 → CF」；
   这种情况数值会全挤在一起，页面会直接提示「区分度不足」。
6. **优选池 & 健康检查**：
   - 上方显示**已验证可用集**（健康检查与自动优选优先使用的 IP）；
   - 下方是**候选域名池** —— 当优选池不够用时，解析这些域名拿到当前 CF 泛播 IP 再扫描可用性。
     保存后即时生效，不需要重新部署。

**健康检查**由 GitHub Actions 每 12 小时跑一次，发现 A 记录指向 1034 或不可达时，
从可用集里自愈。Actions 页可以手动触发「Health Check & Auto Repair」。
（面板里若显示了触发链接，直接点即可。）

### 3.4 首页伪装

**伪装与安全** 选项卡。启用后，没通过隐蔽入口的访客访问根路径 `/` 只会看到一个普通站点页面。

- **隐蔽路径**：进门地址。设了之后 `/__login` 也会先要求走它。
- **进门口令**：URL 里带的口令，用来给浏览器打上「进门标记」。
- **模板**：极简维护页 / 企业官网 / 文章列表 / 下载页 / 自定义 HTML。选「自定义」就整页直出你贴的 HTML。
- **严格模式**：未登录时连 API 数据都拿不到（返回伪装 404）。**对外一定要开。**
- 想自查访客视角，用面板里的「预览伪装页」；浏览器带着进门 cookie 时看到的是真面板，
  这个按钮就是用来绕过自己身份的。

### 3.5 外观主题

**外观主题** 选项卡：

- 点任意色卡即可切换（只影响你这台浏览器）；想让所有访客都用这套，点「设为全站默认」。
- **自动轮换**：三种方式 —— 关闭、每次访问随机、按时轮换（同一时刻所有人看到的是同一套）。
  可以指定参与轮换的主题池，留空 = 全部。
- **自定义主题**：只写要覆盖的 CSS 变量（如 `--accent`、`--bg`、`--font`、`--radius`），其余自动继承。

登录页在伪装开启时**刻意不套主题**，保持中性外观 —— 免得登录页的配色本身成为识别特征。

### 3.6 站点临时访问链接

**临时链接** 选项卡：

1. 上方是开关与默认值：启用、链接前缀、默认有效期、最长天数、默认次数上限、是否记录访问次数。
2. 下方选站点 + 天数 + 次数 → 「生成链接」。
3. 列表里可以直接复制、停用、启用、删除。

**有效期与访问次数任一先到即失效。** 计数与到期判定都在服务端做 —— 写进链接里的东西可以被伪造。

### 3.7 访问统计与数据驾驶舱

**数据驾驶舱** 选项卡。

上半屏是驾驶舱：总请求数、总流量、独立访客、错误数与错误率、有数据的天数、峰值日；
往下是**每日趋势图**（可切换看请求数 / 流量 / 错误数）与**通道排行**（谁在用：站点、代理节点、订阅、临时订阅、管理面板…）。
右上角可切「近 7 / 14 / 30 天」并刷新；把鼠标停在柱子上能看到当天的请求、流量与错误明细。

下半屏是统计设置：

- 打开「启用统计」后才会记录；关闭后不再记录新数据，**已记录的仍然保留**。
  （默认是关闭的，新部署需要手动打开一次。）
- 实现上是**内存里聚合、按间隔批量落盘**，不逐请求写存储，所以开着也不拖慢代理。
- 「记录来访者数」只存来访 IP 的哈希，不存原始 IP。
- 「把管理面板自身的访问也计入」默认关闭：统计是拿来看**访客**的，不该混进自己看面板的次数。
- 保留天数是硬上限（也是驾驶舱能回看的最长跨度），超期数据自动清理。

工具区（同卡片「工具」折叠里）：

- **查看统计数据**：直接看接口返回的原始 JSON（给排障和脚本用）。
- **清空统计数据**：删掉已落盘的记录，配置本身保留。

> **要加一个访问通道？** 改一处就够：`src/scopes.js` 的 `VISIT_SCOPES` 登记表。
> 路径匹配、驾驶舱里的中文名、以及「算不算管理通道（受 record_admin 控制）」都读这张表，
> 所以不用再去前端或统计模块里补一遍映射（补漏了不会报错，只会静默显示成生 id）。
> 表里加一行形如 `{ id: 'newpath', label: '新通道', prefixes: ['/newpath'] }` 即可；
> 需要按 id 分通道（像 `/p/<站点id>` 那样）再加上 `capture: true`。
> 各通道**记什么、留多久、记不记管理访问**属于策略，统一在 `src/stats.js` 的 `SPEC` 里，
> 走面板或环境变量配置。

> 通道口径：`/p/<站点>` 按站点分开统计，`/sub`、`/tsub`、`/edt`（代理入口）、管理面板各自一个通道；
> 伪装页、favicon、robots 这类噪声路径不计入。

### 3.8 限流与防滥用

**配置** 选项卡 → 限流与防滥用。

日常只需要三个字段：**启用**、**窗口内允许请求数**、**时间窗**。
`已登录的请求不限流` 建议保持开启，否则自己用面板时会被自己的阈值挡住。

其余在「进阶选项」里：放行名单（IP / CIDR）、豁免路径、屡犯封禁的阈值与时长。
**如果启用了限流，记得把登录接口加进「豁免路径」**，否则登录页自己会被限流挡掉。

要点：

- 计数在**每个实例的内存**里做，是「单实例级平滑限流」，不是全局精确计数 ——
  限流是防滥用，不是计费，为绝对精确加一次存储往返会把每个正常请求都拖慢。
- **封禁记录会落盘**，跨实例生效，可以在同一张卡的「工具」里查看当前封禁、一键解封。
- 改动生效有几秒的收敛延迟（实例内有短暂缓存），属正常现象，不是没保存上。

### 3.9 告警通知

**配置** 选项卡 → 告警通知。

1. 填 **Webhook 地址**，选推送格式（通用 JSON / 企业微信机器人 / 钉钉机器人）。
2. 打开「启用告警」。
3. 点同一张卡「工具 → 发一条测试告警」，走完整发送链路（绕过冷却），确认通道真的通了。
4. 收不到时看「查看最近发送记录」—— 里面记着每次的跳过原因（冷却中、超出每小时配额、未订阅该事件），
   排查「为什么没收到」先看这里。

注意两点：

- **Webhook 地址是敏感项**：面板只回显「是否已配置」，不发明文。想改就重新填，留空表示不动。
- **同一事件有冷却**（默认 30 分钟），并且有每小时发送上限（默认 20 条），避免一次故障刷爆你的群。
- 不订阅任何事件 = 全部订阅。

---

## 4. 配置放在哪、优先级是什么

每个配置项都按同一套三级取值：

```
面板配置（存在数据库里）  →  环境变量（首次部署的种子）  →  注册表里的默认值（src/settings.js）
```

含义：

- 在面板里存过 → 永远以面板为准，环境变量被忽略。
- 没存过 → 用环境变量的值，并且**第一次读取时会写回存储**，之后就可以在面板里维护了。
- 都没配 → 用注册表里明写的默认值。这些默认值都是与业务无关的中性值，
  项目里没有「取不到就悄悄用一个别人的域名/IP」这种兜底。

所以：**想重置某项配置，就在面板里改成想要的值，不要去删数据库**。
环境变量改了不会立刻生效 —— 面板里已有值时它不参与判断。

各字段对应的环境变量名与默认值，见附录 A（由代码生成，不会过期）。

---

## 5. 出问题怎么查

### 5.1 症状对照表

| 症状 | 多半是什么 | 怎么办 |
|---|---|---|
| 打开域名看到的是一个普通网站 | 首页伪装在生效，你还没进门 | 用隐蔽路径进一次门，或登录后再看；面板里可用「预览伪装页」自查访客视角 |
| `/__login` 打开是 404 | 同上，伪装开启时未进门一律伪装 404 | 先走隐蔽路径 |
| 登录后 API 全返回 401 | 没有登录态，或换了浏览器 | 重新登录；严格模式下未登录拿不到任何数据是设计如此 |
| `/p/<后缀>/` 打开 502 | 目标站不可达，或目标站不允许被反代 | 看面板的错误提示；换目标站验证，或确认上游是否拦了非浏览器请求 |
| 页面能开但样式/图片丢了 | 目标站用了绝对路径或 CDN 域名 | 先确认是不是目标站自身的限制；重写规则是通用规则，不做站点特判 |
| 站点列表能看，但改不动 | 未登录（只读视图） | 登录后重试 |
| 限流开了，自己也被挡住 | `已登录的请求不限流` 关了，或登录接口没进豁免路径 | 配置 → 限流 → 打开登录豁免 / 把登录路径加进豁免路径 |
| 登录页自己返回 429 | 登录接口没在豁免路径里 | 同上；也可先「解除全部封禁」 |
| 面板显示保存成功，但行为没变 | 配置有 3 秒实例内缓存（多实例部署时其它实例最迟 3 秒后跟上），或你只是改了环境变量 | 等几秒再试；确认改的是面板而不是环境变量。面板保存会广播清缓存，同一个实例立即生效 |
| 收不到告警 | 通道没通 / 冷却中 / 未订阅该事件 | 看「最近发送记录」里的跳过原因；先发测试告警 |
| 优选 IP 一直不更新 | 候选域名池为空、或订阅链接失效 | 看优选页的提示；检查订阅链接与候选域名池 |
| 域名解析到 1034 / 打不开 | 优选 IP 失效 | 健康检查会在下个周期自愈，也可点「立即更新优选 IP」 |
| 临时链接打不开 | 到期了，或次数用完了，或已被停用 | 面板列表里能看到状态；重新生成一条 |

### 5.2 能自己跑的验证

在仓库里（本地）可以直接跑自检，不需要连云：

```bash
node tools/check-smoke.mjs       # 功能组合起来还通不通（改链路后必跑）
node tools/check-configui.mjs    # 配置页与代码是否一致（改配置项后必跑）
node tools/check-single-source.mjs  # 一个设定是不是只写了一份（加/改默认值时必跑）
node tools/check-themes.mjs      # 主题
node tools/check-guard.mjs       # 限流 / 告警 / 临时链接
node tools/check-disguise.mjs    # 伪装
```

想在本地把整个面板点一遍：

```bash
node tools/local-dev.mjs 8787
# 管理页 http://127.0.0.1:8787/__admin  口令 dev
```

要在**已部署的实例**上验证：

```bash
node tools/check-live.mjs https://<你的域名> <口令>
```

它会跑完主题、防护与配置接口，并在结束时把改过的配置恢复原样。

---

## 6. 什么能在线改，什么要重新部署

| 想改的东西 | 怎么做 |
|---|---|
| 站点、订阅链接、优选池、伪装、主题、限流、告警、临时链接 | **面板里改，保存即生效** |
| 优选频率 / 池上限 / 探测并发超时 / 订阅严格模式 / CF API 凭据 / 外部数据源地址 / 来访者哈希盐值… | **「配置中心」→「运行参数」里改，保存即生效**（以前这些只能靠环境变量，改完要重新部署） |
| 页面结构、接口、算法 | 改代码 → push → Actions 自动部署 |
| 数据库表结构 | 改 `migrations/` → push，部署时自动应用 |
| 登录口令 | GitHub Secrets 里的 `PASSWORD`，改完需要重新部署一次 |
| 存储后端 / 绑定（`STORAGE_BACKEND`、`DB`、`SITES`、`MEDIA_R2`） | 部署形态，必须和 `wrangler.toml` 的绑定一起改 → 重新部署，面板里改不动也不该改 |

一句提醒：**敏感项（口令、Webhook 地址、进门口令、CF API Token）一律不要在面板之外的公开渠道传**，
面板本身也不会回显它们的明文——凭据类字段只显示「是否已配置」。 

---

## 附录 A · 速查表

> 本节由 `node tools/gen-manual.mjs` 从源码自动生成，与实现保持同步。
> 想改这里的内容，请改 `src/api-catalog.js` 或各模块的 SPEC，然后重新生成；
> CI 会检查两者是否一致，手改这一节会在流水线上红掉。

<!-- BEGIN:GENERATED -->

### A.1 配置项速查

「面板」一列是管理面板里的位置。三处取值优先级：**面板配置 → 环境变量 → 默认值**，面板里存过就以面板为准，所以环境变量只用于首次部署时给个种子。

#### 访问统计（「数据驾驶舱」选项卡）

| 字段 | 面板标签 | 类型 | 默认值 | 环境变量 | 说明 |
|---|---|---|---|---|---|
| `enabled` | 启用统计 | 开关 | 关 | `STATS_ENABLED` | 关闭后不再记录新数据，已有的保留 |
| `retention_days` | 数据保留 | 整数 | 30 | `STATS_RETENTION_DAYS` | 单位：天 |
| `track_visitors` | 记录来访者数 | 开关 | 开 | `STATS_TRACK_VISITORS` | 只记来访 IP 的哈希，不存原始 IP |
| `top_limit` | 面板显示通道数 | 整数 | 10 | `STATS_TOP_LIMIT` | 单位：个 |
| `uv_limit` | 单桶最多记录来访者 | 整数 | 200 | `STATS_UV_LIMIT` | 单位：个 |
| `flush_ms` | 落盘间隔 | 整数 | 15000 | `STATS_FLUSH_MS` | 单位：毫秒 |
| `record_admin` | 把管理面板自身的访问也计入 | 开关 | 关 | `STATS_RECORD_ADMIN` | — |

#### 限流与防滥用（「配置」选项卡）

| 字段 | 面板标签 | 类型 | 默认值 | 环境变量 | 说明 |
|---|---|---|---|---|---|
| `enabled` | 启用限流 | 开关 | 关 | `RATELIMIT_ENABLED` | — |
| `max_requests` | 窗口内允许请求数 | 整数 | 120 | `RATELIMIT_MAX_REQUESTS` | 单位：次 |
| `window_seconds` | 时间窗 | 整数 | 60 | `RATELIMIT_WINDOW_SECONDS` | 单位：秒 |
| `scope` | 计数口径 | 文本 | ip | `RATELIMIT_SCOPE` | — |
| `exempt_authed` | 已登录的请求不限流 | 开关 | 开 | `RATELIMIT_EXEMPT_AUTHED` | 自己用面板时不会被自己的阈值挡住 |
| `message` | 被挡时的提示文案 | 文本 | 请求过于频繁，请稍后再试 | `RATELIMIT_MESSAGE` | — |
| `whitelist` | 放行名单 | 文本 | 空 | `RATELIMIT_WHITELIST` | 每行一个 IP 或 CIDR，例如 203.0.113.0/24 |
| `exempt_paths` | 豁免路径 | 文本 | 空 | `RATELIMIT_EXEMPT_PATHS` | 每行一个路径前缀；建议把登录接口放进来，否则登录页自己会被限流 |
| `ban_enabled` | 启用屡犯封禁 | 开关 | 关 | `RATELIMIT_BAN_ENABLED` | 被挡够次数就把来源封一段时间 |
| `ban_threshold` | 触发封禁的被挡次数 | 整数 | 5 | `RATELIMIT_BAN_THRESHOLD` | 单位：次 |
| `ban_seconds` | 封禁时长 | 整数 | 600 | `RATELIMIT_BAN_SECONDS` | 单位：秒 |

#### 告警通知（「配置」选项卡）

| 字段 | 面板标签 | 类型 | 默认值 | 环境变量 | 说明 |
|---|---|---|---|---|---|
| `enabled` | 启用告警 | 开关 | 关 | `ALERT_ENABLED` | — |
| `webhook_url` | Webhook 地址 | 文本 | （不回显） | `ALERT_WEBHOOK_URL` | — |
| `webhook_type` | 推送格式 | 文本 | generic | `ALERT_WEBHOOK_TYPE` | — |
| `cooldown_minutes` | 同一事件冷却 | 整数 | 30 | `ALERT_COOLDOWN_MINUTES` | 单位：分钟 |
| `events` | 订阅的事件 | 文本 | 空 | `ALERT_EVENTS` | 每行一个，留空 = 全部订阅。可选：login_fail / ratelimit_block / upstream_fail / dns_fail / temp_link_expired |
| `title_prefix` | 标题前缀 | 文本 | 空 | `ALERT_TITLE_PREFIX` | — |
| `max_per_hour` | 每小时最多发送 | 整数 | 20 | `ALERT_MAX_PER_HOUR` | 单位：条 |
| `timeout_ms` | 发送超时 | 整数 | 4000 | `ALERT_TIMEOUT_MS` | 单位：毫秒 |

#### 配置中心（「配置中心」选项卡）

| 字段 | 面板标签 | 类型 | 默认值 | 环境变量 | 说明 |
|---|---|---|---|---|---|
| `pool_limit` | 优选池上限 | 整数 | 30 | `POOL_LIMIT` | 优选池 / 已验证可用集各最多保留多少条；面板写入与运行时读取共用同一上限；单位：条 |
| `domain_pool_limit` | 候选域名池上限 | 整数 | 12 | `DOMAIN_POOL_LIMIT` | 解析成本随条数上升，且 A 记录只留「A 记录条数」那么多条；单位：个 |
| `candidate_limit` | 订阅候选上限 | 整数 | 40 | `SUB_CANDIDATE_LIMIT` | 一次「拉取优选候选」最多返回多少个 IP；单位：个 |
| `sub_strict` | 只保留边缘网络内的候选 | 开关 | 开 | `SUB_STRICT` | 第三方订阅常混入不可达节点；关掉会把它们也当作候选，通常更慢 |
| `sub_ua` | 拉订阅的 User-Agent | 文本 | Mozilla/5.0 | `SUB_UA` | — |
| `max_probe_limit` | 单轮最多探测 | 整数 | 32 | `MAX_PROBE_LIMIT` | 防止池里塞满不可达 IP 时把整次请求拖垮；单位：个 |
| `probe_concurrency` | 探测并发 | 整数 | 12 | `PROBE_CONCURRENCY` | 单位：路 |
| `probe_timeout_ms` | 单次探测超时 | 整数 | 2500 | `PROBE_TIMEOUT_MS` | 单位：毫秒 |
| `latency_enabled` | 按实测延迟排序 | 开关 | 关 | `LATENCY_ENABLED` | 关着的时候优选只挑「能通的」，顺序是谁先探到谁在前；开着会逐个实测延迟再按快慢排。多花几秒，通常换来明显更快的节点 |
| `latency_samples` | 每个目标取样次数 | 整数 | 2 | `LATENCY_SAMPLES` | 取中位数，抵消单次抖动。次数越多越准、也越耗时（会挤占总预算）；单位：次 |
| `latency_budget_ms` | 延迟实测总预算 | 整数 | 8000 | `LATENCY_BUDGET_MS` | 预算耗尽就按已测到的结果排序，绝不把整个请求拖过 Workers 的墙钟；单位：毫秒 |
| `sub_latency_sort` | 订阅按实测延迟排序 | 开关 | 开 | `SUB_LATENCY_SORT` | 客户端通常拿订阅里第一个节点用，所以顺序就是速度。开着会把实测最快的排到最前；关着则原样透传上游订阅的顺序（近似随机）。测量结果有缓存，只有缓存过期后那一次拉取会多花几秒 |
| `sub_latency_ttl_ms` | 延迟缓存有效期 | 整数 | 1800000 | `SUB_LATENCY_TTL_MS` | 缓存期内复用上次实测结果，不重复探测。调大省时间但跟不上网络变化，调小更准但每次拉订阅都要实测一遍；单位：毫秒 |
| `pick_speed_enabled` | 自动优先用浏览器测速的顺序 | 开关 | 开 | `PICK_SPEED_ENABLED` | 服务端只能从 Cloudflare 自己的网络探测，那个「最快」跟你的网络基本无关（实测两个视角的排序近似零相关）。开着就用你在面板「浏览器自动优选」测出来的快慢来排；关着则回到服务端探测的顺序。服务端始终只负责把不通的 IP 剔掉 |
| `pick_speed_ttl_ms` | 浏览器测速结果有效期 | 整数 | 43200000 | `PICK_SPEED_TTL_MS` | 超过这个时间没重新测过就当作没测过，回到服务端顺序。换了网络（回家、出国）旧数字就是错的，所以宁可不用也不能一直信；单位：毫秒 |
| `pick_scan_limit` | 浏览器一次最多测几个 | 整数 | 40 | `PICK_SCAN_LIMIT` | 拉到的候选先截到这个数再逐个测。调大更准但更慢（每个都要打一次请求）；单位：个 |
| `pick_keep` | 测完写进优选池几条 | 整数 | 15 | `PICK_KEEP` | 只留最快的这几条。写入时还会按「优选池上限」再夹一次，两者取小；单位：条 |
| `pick_concurrency` | 浏览器测速并发 | 整数 | 8 | `PICK_CONCURRENCY` | 同时测几个 IP。太大容易被浏览器或网络当成异常流量，反而测不准；单位：路 |
| `pick_timeout_ms` | 单次测速超时 | 整数 | 3500 | `PICK_TIMEOUT_MS` | 单个 IP 等多久算没回应。网络差时调大能少丢一些慢节点，代价是整轮变长；单位：毫秒 |
| `pick_candidate_timeout_ms` | 拉候选的超时 | 整数 | 9000 | `PICK_CANDIDATE_TIMEOUT_MS` | 从订阅链接拉候选最多等多久。订阅站反应慢就调大；超时会退回用当前优选池；单位：毫秒 |
| `hc_slow_evict` | 淘汰明显慢的节点 | 开关 | 开 | `HC_SLOW_EVICT` | 健康检查以前只看通不通，慢到离谱的照样占着 A 记录。开着会把「你测出来明显慢」的节点剔出候选。只用浏览器测速的数字判定，不用服务端探测的（后者跟你的网络无关） |
| `hc_slow_factor` | 慢于最快几倍算慢 | 整数 | 3 | `HC_SLOW_FACTOR` | 相对判据：最快 100ms、这个节点 400ms 才算慢。倍数调越小淘汰越狠；单位：倍 |
| `hc_slow_floor_ms` | 慢的绝对下限 | 整数 | 600 | `HC_SLOW_FLOOR_MS` | 绝对判据：低于这个毫秒数一律不算慢。没有它的话，「最快 30ms、最慢 90ms」也会被判成三倍慢 —— 其实谁都一样快，淘汰纯属自伤；单位：毫秒 |
| `hc_keep_min` | 至少保留几个候选 | 整数 | 2 | `HC_KEEP_MIN` | 剔除慢节点后不够这个数就不剔了。「快的没人可用」比「慢的还在用」糟得多；单位：个 |
| `dns_budget_ms` | 优选总时间预算 | 整数 | 22000 | `DNS_BUDGET_MS` | 必须小于 Workers 的 30 秒墙钟上限，否则请求会被平台直接杀掉；单位：毫秒 |
| `dns_settle_ms` | 写完 A 记录后的等待 | 整数 | 2500 | `DNS_SETTLE_MS` | 等记录生效再自检；调太短会把「已写入但还没生效」误判为失败；单位：毫秒 |
| `max_targets` | A 记录条数 | 整数 | 2 | `MAX_TARGETS` | 多条 A 记录由浏览器自动负载均衡；单位：条 |
| `cf_ip_ranges` | 手工指定边缘 IP 段 | 文本 | 空 | `CF_IP_RANGES` | 每行一个 CIDR，例如 104.16.0.0/13；留空则用下面的数据源自动拉取 |
| `cf_ip_ranges_url` | 边缘 IP 段数据源 | 文本 | https://api.cloudflare.com/client/v4/ips | `CF_IP_RANGES_URL` | 返回 {"result":{"ipv4_cidrs":[...]}}；可换成自建镜像 |
| `ranges_ttl_ms` | 边缘 IP 段缓存时长 | 整数 | 43200000 | `RANGES_TTL_MS` | 单位：毫秒 |
| `doh_url` | 解析候选域名的 DoH 服务 | 文本 | https://cloudflare-dns.com/dns-query | `DOH_URL` | Workers 自身没有 DNS 解析能力，只能借道查询接口 |
| `cf_nets_url` | 官方 IPv4 段列表 | 文本 | https://www.cloudflare.com/ips-v4 | `CF_NETS_URL` | 用于把查不到归属的 IP 标成「任播」，而不是编一个国名 |
| `node_country_anycast` | 任播兜底标注 | 开关 | 开 | `NODE_COUNTRY_ANYCAST` | 命中官方段但查不到国家时标成「任播」（这类 IP 本来就没有单一国家归属） |
| `geoip_batch_url` | IP 归属查询接口 | 文本 | https://api.country.is/ | `GEOIP_BATCH_URL` | 接受 JSON 数组、返回国家代码的批量端点都能替换 |
| `geoip_batch_size` | 单次批量查询条数 | 整数 | 100 | `GEOIP_BATCH_SIZE` | 单位：个 |
| `proxy_host` | 优选目标域名 | 文本 | 空 | `PROXY_HOST` | 自动优选往哪个域名的 A 记录上写；留空则用当前访问的域名（cron 触发时用最后一次访问过的） |
| `proxy_hedge_ms` | 转发竞速对冲延迟 | 整数 | 0 | `PROXY_HEDGE_MS` | 首个上游超过这个时间还没响应，就并发请求第二个，取先到的；0 表示关闭；单位：毫秒 |
| `cf_api_token` | API Token | 文本 | （不回显） | `CF_API_TOKEN` | 需要「Zone:Read + Analytics:Read + DNS:Edit」权限；留空表示不修改已配置的值 |
| `cf_zone_id` | Zone ID | 文本 | 空 | `CF_ZONE_ID` | — |
| `cf_account_id` | Account ID | 文本 | 空 | `CF_ACCOUNT_ID` | 留空则用 token 自动列出账户取第一个 |
| `cf_api_base` | API 端点 | 文本 | https://api.cloudflare.com/client/v4 | `CF_API_BASE` | — |
| `worker_script` | Worker 脚本名 | 文本 | any-proxy | `WORKER_SCRIPT` | 取 wrangler.toml 的 name；同一账户常挂着多个脚本，所以要指明看哪一个 |
| `analytics_ttl_ms` | 用量数据缓存时长 | 整数 | 300000 | `ANALYTICS_TTL_MS` | GraphQL 分析查询按次配额计费，宁可看几分钟前的数也不刷爆配额；单位：毫秒 |
| `cf_api_timeout_ms` | CF 接口超时 | 整数 | 8000 | `CF_API_TIMEOUT_MS` | 单位：毫秒 |
| `gh_actions_url` | 手动触发健康检查的链接 | 文本 | 空 | `GH_ACTIONS_URL` | 改成你自己仓库的 Actions 地址；留空则面板不显示该按钮 |
| `stats_salt` | 来访者哈希盐值 | 文本 | （不回显） | `STATS_SALT` | 只用于给来访 IP 做哈希，原始 IP 不出内存；留空则自动生成并长期保持不变 |
| `enabled` | 启用持久缓存 | — | 开 | — | 关闭后不再往 R2 写入分片，已缓存的不受影响 |
| `ttlDays` | 分片保留天数 | — | 7 | — | 单位：天 |
| `maxObjectMB` | 单分片缓存上限 | — | 64 | — | 单位：MB |
| `maxTotalMB` | 缓存总量上限 | — | 10240 | — | 0 表示不限；超出后按到期时间淘汰最早的对象；单位：MB |

#### 代理节点与订阅（「代理节点」选项卡）

| 字段 | 面板标签 | 类型 | 默认值 | 环境变量 | 说明 |
|---|---|---|---|---|---|
| `node_uuid` | 节点 ID（UUID） | 文本 | 空 | `UUID` | 订阅链接里的用户 ID。改它会让已发出的链接全部失效，需要重新复制节点链接 |
| `node_host` | 节点地址 | list | — | `HOST` | 每行一个域名，第一个进入订阅链接；留空则用客户端实际访问的域名 |
| `node_path` | 节点路径 | 文本 | / | `PATH` | 客户端连接的路径，必须以 / 开头；反向代理参数会自动追加在它后面 |
| `node_protocol` | 协议类型 | 文本 | vless | — | — |
| `node_transport` | 传输协议 | 文本 | ws | — | — |
| `node_fingerprint` | TLS 指纹（fp） | 文本 | chrome | — | — |
| `node_sub_name` | 订阅名称 | 文本 | edgetunnel | — | 客户端里显示的订阅名，也是节点备注的默认值 |
| `node_sub_update_hours` | 客户端订阅更新间隔 | 整数 | 3 | — | 写进订阅响应的 Profile-Update-Interval。引擎按「小时」解释这个值，所以面板也用小时；单位：小时 |
| `sub_url` | 订阅链接 | 文本 | 空 | `SUB_URL` | 完整 URL（http(s)://…）或以 / 开头的路径（如 /tsub/xxx）；留空则回退到本机 /sub |
| `node_tag_enabled` | 给节点备注补国家 | 开关 | 开 | `NODE_COUNTRY_TAG` | 关闭后订阅原样输出，不产生任何外部查询 |
| `node_tag_style` | 标注样式 | 文本 | cn-code | `NODE_COUNTRY_STYLE` | cn-code 美国【US】／flag-name 🇺🇸美国／name 美国／code US／flag 🇺🇸 |

#### 优选 IP 与 DNS（「优选 IP」选项卡）

| 字段 | 面板标签 | 类型 | 默认值 | 环境变量 | 说明 |
|---|---|---|---|---|---|
| `dns_interval_minutes` | 自动优选频率 | 整数 | 720 | `DNS_INTERVAL` | 定时任务按这个间隔执行优选；改完立即生效，不等下一个周期；单位：分钟 |
| `preferred_ips` | 优选 IP 池 | list | — | `PREF_IPS` | 每行一个 IPv4；「浏览器自动优选」测速后也存这里，自动优选优先从它取候选 |
| `pool_good_ips` | 已验证可用集 | list | — | — | 健康检查与自动优选写回的「确实通」的 IP，优先级高于上面的优选池 |
| `pref_domains` | 候选域名池 | list | — | `PREF_DOMAINS` | 每行一个域名；解析它们的 A 记录来补充候选 IP，留空则不启用这一路候选 |

#### 站点临时访问链接（「临时链接」选项卡）

| 字段 | 面板标签 | 类型 | 默认值 | 环境变量 | 说明 |
|---|---|---|---|---|---|
| `enabled` | 启用临时链接 | 开关 | 关 | `SHARE_ENABLED` | 关闭后已发出的链接也立即失效 |
| `path_prefix` | 链接前缀 | 文本 | /s | `SHARE_PATH_PREFIX` | 生成的链接形如 <前缀>/<token> |
| `default_days` | 默认有效期 | 整数 | 1 | `SHARE_DEFAULT_DAYS` | 单位：天 |
| `max_days` | 允许的最长天数 | 整数 | 30 | `SHARE_MAX_DAYS` | 单位：天 |
| `default_max_hits` | 默认次数上限 | 整数 | 0 | `SHARE_DEFAULT_MAX_HITS` | 0 表示不限次数，只看到期时间；单位：次 |
| `count_hits` | 记录访问次数 | 开关 | 开 | `SHARE_COUNT_HITS` | — |

### A.2 接口速查

登录后的管理接口。未登录访问一律拿不到数据 —— 首页伪装开启时更会直接返回伪装 404，这是设计如此，不是故障。

| 方法 | 路径 | 登录 | 说明 | 面板位置 |
|---|---|---|---|---|
| GET | `/__api/stats-config` | 是 | 内存里聚合、按间隔批量落盘，不逐请求写存储，所以开着也不拖慢代理。 | 「数据驾驶舱」 |
| GET | `/__api/stats` | 是 | 按天聚合的通道排行、流量（实际传输字节）、媒体流量、传输中断与来访数（驾驶舱用的就是这份数据） | 「数据驾驶舱」 |
| POST | `/__api/stats/clear` | 是 | 删掉已落盘的全部访问记录，保留统计设置本身 | 「数据驾驶舱」 |
| GET | `/__api/cf-analytics` | 是 | 按天请求数/带宽/缓存命中 + 近 24h 状态码分布与 Top 路径 + 当前 Worker 脚本近 30 天调用（5 分钟缓存，单源失败自动降级并注明） | 「CF 用量」 |
| GET | `/__api/ratelimit` | 是 | 计数在实例内存里做（单实例级平滑限流），只有封禁记录落盘，跨实例生效。 | 「配置」 |
| GET | `/__api/ratelimit/bans` | 是 | 跨实例生效的封禁记录，到期自动解除 | 「配置」 |
| POST | `/__api/ratelimit/clear` | 是 | 一键解封，用于确认是自己误封之后立刻恢复 | 「配置」 |
| GET | `/__api/alert` | 是 | Webhook 地址本身是一条凭据，面板只回显「是否已配置」，留空表示不修改。 | 「配置」 |
| POST | `/__api/alert/test` | 是 | 绕过冷却走完整发送链路，用来确认通道真的配对了 | 「配置」 |
| GET | `/__api/alert/recent` | 是 | 含跳过原因，排查「为什么没收到」先看这里 | 「配置」 |
| GET | `/__api/preferred-candidates` | 否 | 按 Cloudflare 官方 IP 段过滤后，从订阅里抽出的候选 IP | 「配置」 |
| GET | `/__api/speedtest` | 否 | 从 Worker 侧测各站点上游的响应延迟，用于判断慢在链路还是源站 | 「配置」 |
| GET | `/__api/sites` | 否 | 所有站点及其访问前缀 | 「站点」 |
| POST | `/__api/sites` | 是 | 名称 + 网址即可，访问后缀留空自动生成 | 「站点」 |
| PUT | `/__api/sites/<id>` | 是 | 改名称 / 后缀 / 目标地址，以及站点模式字段（proxyMode 取注册表键、mediaCacheAuthBind 盗链保护、mediaSkipDetailLog 媒体日志开关、aiKey/aiKeys AI 中转配置）；后缀变化时旧链接会一并迁移 | 「站点」 |
| DELETE | `/__api/sites/<id>` | 是 | 删除后 /p/<id>/ 立即失效 | 「站点」 |
| GET | `/__api/settings` | 是 | 全部「非部署形态」的可运营参数：字段表在 src/settings.js 一处声明，界面与文档都由它生成。三处取值优先级为「面板配置 → 环境变量种子 → 规范默认值」，保存后立即生效、无需重新部署。不在这里的只有部署形态项（存储后端与绑定、进站口令、代理引擎密钥）——它们必须与 wrangler.toml 一起改。 | 「配置中心」 |
| GET | `/__api/site-modes` | 是 | 站点类型标签 / 徽标 / 说明 / 执行引擎（KV 可编辑，未配置用默认；内置 normal/media/ai 不可删、引擎不可改，自定义模式可增删改） | 「配置中心」 |
| GET | `/__api/r2-cache` | 是 | 流媒体分片持久缓存的策略：总开关 / 保留天数 / 单分片上限 MB / 缓存总量上限 MB，KV 可编辑、保存即生效 | 「配置中心」 |
| POST | `/__api/r2-cache` | 是 | 不等定时周期，马上删除当前已过期的媒体分片 | 「配置中心」 |
| GET | `/__api/node-config` | 是 | 订阅里出现的节点 ID / 地址 / 路径 / 协议 / 传输 / 指纹与订阅名称、更新间隔。取值与代理引擎共用同一份配置文档，保存后下一个订阅请求立即按新值输出；改节点 ID 会让已发出的订阅链接失效，需要重新复制节点链接。（订阅 TOKEN 由「节点地址 + 节点 ID」派生，不是独立配置，所以这里不提供填写处。） | 「代理节点」 |
| GET | `/__api/sub-config` | 是 | 浏览器优选从这里拉候选 IP；留空则用本机 /sub。（值与校验来自统一运行参数表，编辑入口就在本页，只此一处） | 「代理节点」 |
| GET | `/__api/node-tag` | 是 | 给订阅节点的备注补上 IP 归属国家，例如 CF 电信优选 \| 美国【US】。主订阅 /sub 与临时订阅 /tsub/<id> 同一套规则；关闭后订阅原样输出，不产生任何外部查询。国家查询结果长期缓存，同一个 IP 只真正查询一次。 | 「代理节点」 |
| GET | `/__api/tempsubs` | 是 | 限时有效的临时订阅记录 | 「代理节点」 |
| POST | `/__api/tempsubs` | 是 | 独立 UUID，到期自动失效 | 「代理节点」 |
| GET | `/__api/dns-config` | 是 | 定时任务按这个间隔执行一次优选；保存后立即生效，不等下一个周期 | 「优选 IP」 |
| POST | `/__api/dns-run` | 是 | 不等定时任务，马上测通并改写 A 记录（不可用会自动回滚） | 「优选 IP」 |
| GET | `/__api/preferred-ips` | 是 | 自动优选优先从这里取候选；条数上限沿用「配置中心 → 优选与候选」里的「优选池上限」 | 「优选 IP」 |
| POST | `/__api/preferred-ips` | 是 | 并发测通当前优选池里的 IP，把可用的写入 A 记录并自检（不修改池子内容） | 「优选 IP」 |
| GET | `/__api/pool-config` | 是 | 「已验证可用集」是自动优选与健康检查写回的、确实通的 IP（优先级高于优选池）；「候选域名池」是解析 A 记录来补充候选 IP 的域名清单，留空则不启用这一路 | 「优选 IP」 |
| GET | `/__api/pick-speed` | 是 | 存「IP → 毫秒」这张表：面板「浏览器自动优选」从你的网络实测后写入，自动优选据此排序。服务端只能从 Cloudflare 自己的网络探测，那个快慢跟你的网络基本无关，所以顺序必须由浏览器这边定；服务端只负责剔掉不通的。有效期在「配置中心 → 优选与候选」里调 | 「优选 IP」 |
| GET | `/__api/pool-slow` | 是 | 用户侧实测明显慢、因此不进本轮候选的 IP。GET 读名单，POST 按当前测速结果重算。判定只用浏览器测速的数字（服务端探测的快慢与用户网络无关，拿它淘汰会误杀最快的节点）；样本不足、结果过期、开关关闭时一律不判定并清空名单。阈值在「配置中心 → 优选与候选」里调 | 「优选 IP」 |
| POST | `/__api/latency-probe` | 是 | 逐个实测候选 IP 的真实响应延迟、按快慢排好。不写任何配置，只回答「谁更快」；不传 targets 时测当前优选池。取样次数与总预算在「配置中心 → 优选与候选」里调 | 「优选 IP」 |
| GET | `/__api/themes` | 是 | 内置 10 套 + 自定义；default_theme 是全站默认（访客未自选时生效） | 「外观主题」 |
| POST | `/__api/themes/custom` | 是 | 只写要覆盖的 CSS 变量，其余自动继承 | 「外观主题」 |
| GET | `/__api/share-config` | 是 | 链接前缀可改（默认 /s）；不能占用 /p /__ /edt 等保留段。 | 「临时链接」 |
| GET | `/__api/shares` | 是 | 含剩余时间与访问次数，面板里可直接复制 / 停用 / 删除 | 「临时链接」 |
| GET | `/__api/disguise` | 是 | 模板 / 文案 / 隐蔽入口 / 严格模式（口令仅返回 has_token，写回才覆盖） | 「伪装与安全」 |

<!-- END:GENERATED -->
